"""Project runner — Docker-isolated whole-project live preview.

Detects a workspace's project type and runs the WHOLE project as a web app in a
container, exposed for iframe preview. See ADR-018 / docs/design.
"""
